# Godot 4+ specific ignores
.godot/

# These file paths are not in the Godot project itsel, they are not working
#/* ## THIS ONE JUST BREAKS STUFF, KEEPS US FROM ADDING NEW FILES
#!/uid_cache.bin

# Godot-specific ignores
.import/
export.cfg
export_presets.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json
